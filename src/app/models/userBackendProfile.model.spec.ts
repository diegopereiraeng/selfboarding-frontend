import { UserBackendProfile } from './userBackendProfile.model';

describe('UserBackendProfile', () => {
    it('should create an instance', () => {
        expect(new UserBackendProfile()).toBeTruthy();
    });
});
