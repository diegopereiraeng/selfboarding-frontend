import { UserBackendRole } from './userBackendRole.model';

describe('UserBackendRole', () => {
    it('should create an instance', () => {
        expect(new UserBackendRole()).toBeTruthy();
    });
});
